import { LoginForm } from './LoginForm';
import { RedirectButton } from './redirectButton';

const Login = () => {
  return (
    <>
      <LoginForm />
      <RedirectButton />
    </>
  );
};

export default Login;
